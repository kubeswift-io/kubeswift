# Go
*.exe
*.test
*.out
vendor/

# Rust
/target/
**/target/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Built binaries (top-level only — never the cmd/swiftctl/ source)
/swiftctl
/bin/swiftctl
/controller-manager
/gpu-discovery
/kubeswift-dra-driver
/webhook-server
/snapshot-stager
/snapshot-s3

# Spike scratch directories (per-spike scripts + JSONL run logs).
# Spike scripts are scratch; their findings are committed under
# docs/design/<phase>-spike.md instead. Convention since
# Phase 3a (2026-05).
/spike/
# Build artifacts (go build output in repo root)
/snapshot-s3
